# this is part of RuneAudio
# pcm output device to pipe spotify connect output to an owntone fifo pipe
pcm.owntonescfifo {
 type plug
 slave {
  # type rate # by not specifying 'type rate' the rate converter will only be used when required
  pcm "owntonescFIFO"
  format S16_LE # owntone format
  rate 44100 # owntone rate
  channels 2 # owntone channels
 }
 hint {
  show {
   @func refer
   name defaults.namehint.extended
  }
  description "pcm output device to pipe spotify connect output to an owntone fifo pipe with rate conversion (S16_LE, 44.1kHz)"
 }
}
pcm.owntonescFIFO {
 type file
 slave.pcm null
 file "/run/owntone/music/pipe_sc.fifo" # the sc owntone fifo file name
 format "raw"
 hint {
  show {
   @func refer
   name defaults.namehint.extended
  }
  description "pcm output device to pipe spotify connect output to an owntone fifo pipe without rate conversion"
 }
}
